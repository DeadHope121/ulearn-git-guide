# S3. РЎСЃС‹Р»РєРё

> HEAD вЂ” С‚РµРєСѓС‰Р°СЏ СЃСЃС‹Р»РєР°, tag вЂ” С„РёРєСЃРёСЂРѕРІР°РЅРЅР°СЏ СЃСЃС‹Р»РєР°, branch вЂ” РґРІРёР¶СѓС‰Р°СЏСЃСЏ Р·Р° HEAD СЃСЃС‹Р»РєР°

> checkout вЂ” РїРµСЂРµРјРµС‰РµРЅРёРµ РЅР° РІРµС‚РєСѓ РёР»Рё РєРѕРјРјРёС‚, reset вЂ” РїРµСЂРµРјРµС‰РµРЅРёРµ СЃ РІРµС‚РєРѕР№ РЅР° РєРѕРјРјРёС‚

> Р’РёРґРЅРѕ С‚Рѕ, РЅР° С‡С‚Рѕ РµСЃС‚СЊ СЃСЃС‹Р»РєРё, РѕСЃС‚Р°Р»СЊРЅРѕРµ вЂ” РјСѓСЃРѕСЂ

- `git tag` вЂ” РІС‹РІРµСЃС‚Рё СЃРїРёСЃРѕРє С‚РµРіРѕРІ
- `git tag <tagname>` вЂ” СЃРѕР·РґР°С‚СЊ С‚РµРі
- `git tag -d <tagname>` вЂ” СѓРґР°Р»РёС‚СЊ С‚РµРі
- `git branch --sort=-committerdate` вЂ” РІС‹РІРµСЃС‚Рё СЃРїРёСЃРѕРє Р»РѕРєР°Р»СЊРЅС‹С… РІРµС‚РѕРє РѕС‚ РЅРѕРІС‹С… Рє СЃС‚Р°СЂС‹Рј
- `git branch <branchname>` вЂ” СЃРѕР·РґР°С‚СЊ РІРµС‚РєСѓ
- `git branch -f <branchname> <to_commit>` вЂ” СЃРѕР·РґР°С‚СЊ РёР»Рё РїРµСЂРµРјРµСЃС‚РёС‚СЊ РІРµС‚РєСѓ
- `git branch -M <newbranch>` вЂ” РїРµСЂРµРёРјРµРЅРѕРІР°С‚СЊ С‚РµРєСѓС‰СѓСЋ РІРµС‚РєСѓ, РґР°Р¶Рµ РµСЃР»Рё РЅРѕРІРѕРµ РёРјСЏ СѓР¶Рµ Р·Р°РЅСЏС‚Рѕ
- `git branch -d <branchname>` вЂ” СѓРґР°Р»РёС‚СЊ РІРµС‚РєСѓ
- `git branch -D <branchname>` вЂ” СѓРґР°Р»РёС‚СЊ РІРµС‚РєСѓ, РґР°Р¶Рµ РµСЃР»Рё РєР°РєРёРµ-С‚Рѕ РєРѕРјРјРёС‚С‹ Р±СѓРґСѓС‚ СЃРєСЂС‹С‚С‹
- `git checkout -d <commit>` РёР»Рё `git switch --detach <commit>` вЂ” РїРµСЂРµРјРµСЃС‚РёС‚СЊ HEAD РЅР° РєРѕРјРјРёС‚, РїСЂРёС‡РµРј РїРѕР»СѓС‡РёС‚СЃСЏ detached HEAD
- `git checkout <branch>` РёР»Рё `git switch <branch>` вЂ” РїРµСЂРµРјРµСЃС‚РёС‚СЊ HEAD РЅР° РІРµС‚РєСѓ
- `git checkout -b <new_branch>` = `git checkout -b <new_branch> HEAD` РёР»Рё `git switch -c <new_branch>` вЂ” СЃРѕР·РґР°С‚СЊ РІРµС‚РєСѓ Рё РїРµСЂРµР№С‚Рё РЅР° РЅРµРµ
- `git reset --hard <commit>` вЂ” РїРµСЂРµРјРµСЃС‚РёС‚СЊ HEAD Рё С‚РµРєСѓС‰СѓСЋ РІРµС‚РєСѓ РЅР° `<commit>`
- `git log --oneline --decorate --graph` вЂ” РІС‹РІРµСЃС‚Рё РёСЃС‚РѕСЂРёСЋ РєРѕРјРјРёС‚РѕРІ РѕС‚ HEAD РІ РІРёРґРµ РґРµСЂРµРІР°
- `git log --oneline --decorate --graph --all` вЂ” РІС‹РІРµСЃС‚Рё РёСЃС‚РѕСЂРёСЋ РєРѕРјРјРёС‚РѕРІ РѕС‚ РІСЃРµС… СЃСЃС‹Р»РѕРє РІ РІРёРґРµ РґРµСЂРµРІР°
- `git log --oneline --decorate --graph --all --reflog` вЂ” РІС‹РІРµСЃС‚Рё РёСЃС‚РѕСЂРёСЋ РєРѕРјРјРёС‚РѕРІ РѕС‚ РІСЃРµС… СЃСЃС‹Р»РѕРє Рё РІСЃРµС… РЅРµРґР°РІРЅРёС… РїРѕР»РѕР¶РµРЅРёР№ HEAD РІ РІРёРґРµ РґРµСЂРµРІР°
- `git reflog show <ref>` вЂ” РїРѕРєР°Р·Р°С‚СЊ Р»РѕРі РґРµР№СЃС‚РІРёР№ СЃРѕ СЃСЃС‹Р»РєРѕР№
- `git reflog` = `git reflog show HEAD` вЂ” РїРѕРєР°Р·Р°С‚СЊ Р»РѕРі РґРµР№СЃС‚РІРёР№ СЃ HEAD
- `git gc` вЂ” СѓРґР°Р»РёС‚СЊ РЅРµРЅСѓР¶РЅС‹Рµ С„Р°Р№Р»С‹ Рё РѕРїС‚РёРјРёР·РёСЂРѕРІР°С‚СЊ Р»РѕРєР°Р»СЊРЅС‹Р№ СЂРµРїРѕР·РёС‚РѕСЂРёР№
- `<ref>~N` вЂ” 1-С‹Р№ СЂРѕРґРёС‚РµР»СЊ РёР· N-РѕРіРѕ РїРѕРєРѕР»РµРЅРёСЏ, РЅР°РїСЂРёРјРµСЂ, `HEAD~3`
- `<ref>^N` вЂ” N-С‹Р№ СЂРѕРґРёС‚РµР»СЊ РїСЂРµРґС‹РґСѓС‰РµРіРѕ РїРѕРєРѕР»РµРЅРёСЏ, РЅР°РїСЂРёРјРµСЂ, `HEAD^1^2`
- `<ref>@{N}` вЂ” N-С‹Р№ РїСЂРµРґС€РµСЃС‚РІРµРЅРЅРёРє РїРѕ reflog, РЅР°РїСЂРёРјРµСЂ, `HEAD@{5}`
